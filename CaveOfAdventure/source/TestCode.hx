/*
        // trace("Possible sacrifice buttons: "+possibleSacrificeButtons.length);
        // trace("Copied buttons: "+copyArray.length);
        // trace("Buttons left: "+copyArray.length);
        // rand.shuffle(possibleSacrificeButtons);
            trace("Adding second jump.");
*/
